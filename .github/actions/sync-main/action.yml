name: "Sync Main"
description: "Syncs main branch to next"

runs:
  using: "composite"
  steps:
    - name: Sync main to the next
      run: yarn run sync-main
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
