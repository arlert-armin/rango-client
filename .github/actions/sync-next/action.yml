name: "Sync Next"
description: "Syncs next with main"

runs:
  using: "composite"
  steps:
    - name: Sync Next
      run: yarn run sync-next
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
